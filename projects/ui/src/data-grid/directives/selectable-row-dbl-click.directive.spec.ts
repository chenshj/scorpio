import { SelectableRowDblClickDirective } from './selectable-row-dbl-click.directive';

describe('SelectableRowDblClickDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectableRowDblClickDirective();
    expect(directive).toBeTruthy();
  });
});
