import { ReorderableRowDirective } from './reorderable-row.directive';

describe('ReorderableRowDirective', () => {
  it('should create an instance', () => {
    // const directive = new ReorderableRowDirective();
    // expect(directive).toBeTruthy();
  });
});
