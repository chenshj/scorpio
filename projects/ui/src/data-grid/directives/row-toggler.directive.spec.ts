import { RowTogglerDirective } from './row-toggler.directive';

describe('RowTogglerDirective', () => {
  it('should create an instance', () => {
    const directive = new RowTogglerDirective();
    expect(directive).toBeTruthy();
  });
});
