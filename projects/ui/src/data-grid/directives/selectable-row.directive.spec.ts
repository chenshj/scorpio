import { SelectableRowDirective } from './selectable-row.directive';

describe('SelectableRowDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectableRowDirective();
    expect(directive).toBeTruthy();
  });
});
