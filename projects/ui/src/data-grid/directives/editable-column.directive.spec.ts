import { EditableColumnDirective } from './editable-column.directive';

describe('EditableColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableColumnDirective();
    expect(directive).toBeTruthy();
  });
});
