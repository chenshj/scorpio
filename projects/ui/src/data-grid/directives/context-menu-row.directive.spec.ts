import { ContextMenuRowDirective } from './context-menu-row.directive';

describe('ContextMenuRowDirective', () => {
  it('should create an instance', () => {
    const directive = new ContextMenuRowDirective();
    expect(directive).toBeTruthy();
  });
});
