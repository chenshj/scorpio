import { ScorpioTemplateDirective } from './scorpio-template.directive';

describe('ScorpioTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ScorpioTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
